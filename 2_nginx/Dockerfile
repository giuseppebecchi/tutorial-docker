# Use the official NGINX image from the Docker Hub
FROM nginx:alpine

# Copy the web pages into the image
COPY web_pages/ /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# NGINX runs by default with the provided configuration
